package com.global.meter.service;

import org.springframework.stereotype.Service;


@Service
public class LoggerService {
/*
	@Autowired
	LogsRepository logsRepository;
	
	public void saveLog(DevicesInfo devicesInfo, String deviceSNO, String cmdId, String cmdName, String status, String reason)
	{
		Logs logs = new Logs(deviceSNO, String.valueOf(System.currentTimeMillis()),
				cmdId, cmdName, status, new Date(System.currentTimeMillis()),
				devicesInfo.getDcu_serial_number(), devicesInfo.getDt_name(),
				devicesInfo.getFeeder_name(), devicesInfo.getOwner_name(),
				devicesInfo.getSubdevision_name(), devicesInfo.getSubstation_name(), reason);
		logsRepository.save(logs);
	}
	*/
}
